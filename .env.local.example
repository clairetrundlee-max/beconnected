# Optional: Mapbox public token for the Search tab map (light style + yellow pins).
# Without this, the Search screen shows a grid placeholder with mock pin positions.
# Get a token at https://account.mapbox.com/
NEXT_PUBLIC_MAPBOX_TOKEN=
